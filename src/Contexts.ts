import * as React from 'react';
import type { MapState } from './index.d';
export const MapStateContext = React.createContext<MapState>({} as MapState);
//export const DimensionsContext = React.createContext<Dimensions>({} as Dimensions);
